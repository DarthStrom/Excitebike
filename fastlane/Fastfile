fastlane_version "1.66.0"

default_platform :ios

platform :ios do

  before_all do
    ensure_git_status_clean
  end

  desc "Run all the tests"
  lane :test do

    # TODO: enable this once it works in travis
    # scan(device: "iPhone 6 (8.1)")

    xctest(
        scheme: 'Excitebike',
        destination: "name=iPhone 6,OS=8.1",
        reports: [{
            report: 'html',
            output: "fastlane/test_output/report.html"
        }]
    )
  end
end
